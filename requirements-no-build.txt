# No Build Requirements - Only Pre-compiled Wheels
# This file contains only packages that don't require compilation

# Core FastAPI
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# HTTP clients
requests==2.31.0
httpx==0.25.0

# Environment
python-dotenv==1.0.0

# Production
gunicorn==21.2.0
python-multipart==0.0.6
aiofiles==23.2.0

# Basic packages only - no geospatial for now
# We'll add geospatial packages after basic deployment works

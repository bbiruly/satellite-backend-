# Simple Requirements - No Geospatial for Initial Deployment
# Deploy basic FastAPI first, then add geospatial packages

# Core FastAPI
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# HTTP clients
requests==2.31.0
httpx==0.25.0

# Environment
python-dotenv==1.0.0

# Production
gunicorn==21.2.0
python-multipart==0.0.6
aiofiles==23.2.0

# Basic packages only - no geospatial for now
